<menu
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/action"
        android:title="action"
        android:icon="@drawable/ic_menu"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_view_user_list"
                android:icon="@drawable/ic_view_list"
                android:title="View list user"
                />
            <item
                android:id="@+id/action_view_student_list"
                android:icon="@drawable/ic_view_list"
                android:title="View list student"
                />
            <item
                android:id="@+id/action_add_user"
                android:icon="@drawable/ic_add_user"
                android:title="Add user"
                />
            <item
                android:id="@+id/action_add_student"
                android:icon="@drawable/ic_add_student"
                android:title="Add student"
                />
            <item
                android:id="@+id/action_profile"
                android:icon="@drawable/ic_person"
                android:title="Profile"
                />
            <item
                android:id="@+id/action_history"
                android:icon="@drawable/ic_history"
                android:title="History"
                />
            <item
                android:id="@+id/action_logout"
                android:icon="@drawable/ic_logout_24"
                android:title="Logout"
                />
        </menu>
    </item>
</menu>